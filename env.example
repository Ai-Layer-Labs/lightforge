# RCRT Environment Configuration Template
# Copy this file to .env and fill in your values

# =============================================================================
# ENCRYPTION (Optional but recommended for production)
# =============================================================================
# Base64-encoded local encryption key for secrets
# Generate with: openssl rand -base64 32
LOCAL_KEK_BASE64=your-base64-encoded-key-here

# =============================================================================
# LLM/AI INTEGRATION (Optional - for AI features)
# =============================================================================
# OpenRouter API Key for LLM access
# Get from: https://openrouter.ai/keys
OPENROUTER_API_KEY=your-openrouter-api-key-here

# OpenRouter HTTP headers (optional)
OPENROUTER_REFERER=your-site-url-here
OPENROUTER_SITE_TITLE=your-site-title-here

# =============================================================================
# DATABASE (Automatically configured by docker-compose)
# =============================================================================
# These are set automatically by docker-compose.yml
# DB_URL=postgres://postgres:postgres@db:5432/rcrt
# NATS_URL=nats://nats:4222

# =============================================================================
# AUTHENTICATION (Pre-configured for development)
# =============================================================================
# These are pre-configured in docker-compose.yml for development
# In production, generate new keys with:
# openssl genpkey -algorithm RSA -out private_key.pem -pkcs8 -pkeyopt rsa_keygen_bits:2048
# openssl rsa -pubout -in private_key.pem -out public_key.pem

# =============================================================================
# SERVICE CONFIGURATION (Advanced)
# =============================================================================
# Hygiene runner settings (optional - defaults are fine)
# HYGIENE_ENABLED=true
# HYGIENE_INTERVAL_SECONDS=30
# HYGIENE_HEALTHCHECK_TTL_MINUTES=5
# HYGIENE_TEMP_DATA_TTL_HOURS=24
# HYGIENE_AGENT_IDLE_HOURS=48

# =============================================================================
# DEVELOPMENT SETTINGS
# =============================================================================
# Rust logging level
# RUST_LOG=info

# Next.js settings (for builder UI)
# NEXT_PUBLIC_RCRT_URL=http://localhost:8081
# NEXT_PUBLIC_WORKSPACE=workspace:builder
