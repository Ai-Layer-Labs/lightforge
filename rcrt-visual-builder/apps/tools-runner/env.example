# RCRT Tools Runner Configuration

# RCRT Connection
RCRT_BASE_URL=http://localhost:8081
RCRT_PROXY_URL=/api/rcrt
TOKEN_ENDPOINT=/api/auth/token
WORKSPACE=workspace:tools

# Deployment Mode: 'docker', 'local', 'electron'
DEPLOYMENT_MODE=local

# Tool Configuration
ENABLE_BUILTIN_TOOLS=true
ENABLE_LANGCHAIN_TOOLS=true
ENABLE_TOOL_UI=true

# API keys must be stored in RCRT Secrets, not here.
# Create secrets with names like:
#  - SERPAPI_API_KEY
#  - OPENAI_API_KEY
#  - ANTHROPIC_API_KEY
#  - BRAVE_SEARCH_API_KEY
#  - GOOGLE_SEARCH_API_KEY
#  - GOOGLE_CSE_ID
