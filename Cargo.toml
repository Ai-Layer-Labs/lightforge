[workspace]
members = [
  "crates/rcrt-core",
  "crates/rcrt-server"
]
resolver = "2"

# Keep non-Rust folders out of the Docker build context to avoid multi-GB transfers
# Build image uses only crates/ and migrations/ copied in Dockerfile


 
